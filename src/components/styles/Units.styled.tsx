import styled from 'styled-components';

export const StyledUnits = styled.div`
    .units__section {
        width: 100vw;
        
        .units__content {
            width: 90vw;
            margin: 0 auto;
        }
    }
`